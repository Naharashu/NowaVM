# Лінкер

NowaVM має власний лінкер для власних виконавчих файлів. Its called 'nwld' or NowaLD.

Щоб зібрати лінкер, який має назву nwld, просто скомпіюйте та вставте цю команду(ви повинні бути у папці з вихідним кодом NowaVM):
```
make linker
./nwld -v # надрукує версію
```
