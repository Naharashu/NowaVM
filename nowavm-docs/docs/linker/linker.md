# Linker

NowaVM have simple linker for own executables. Its called 'nwld' or NowaLD.

To build it just paste this in terminal(needed git source of NowaVM installed):
```
make linker
./nwld -v # prints version
```
